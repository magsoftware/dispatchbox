# Keep the crash/reclaim check fast while leaving enough time to observe the new token.
LOAD_LEASE_SECONDS="${LOAD_LEASE_SECONDS:-3}"
LOAD_TIMEOUT_SECONDS="${LOAD_TIMEOUT_SECONDS:-30}"
LOAD_RECOVERY_HANDLER_DELAY_MS="${LOAD_RECOVERY_HANDLER_DELAY_MS:-200}"
